# @package _global_
defaults:
  - override /hydra:
      - cluster

save_configs: True
output: "/scratch/outputs/data/${now:%Y%m%d}-tng50-1.{}.2D.{}.0000.1000.hdf5"
source: /cluster/projects/ska
num_samples:
  - 1000
snapshots:
  - 91
