# @package _global_
defaults:
  - override /hydra:
      - cluster

save_configs: True
output: "/scratch/outputs/data/${now:%Y%m%d}-tng50-1.{}.2D.{}.1000.2000.hdf5"
source: /cluster/projects/ska
num_samples:
  - 1000
  - 2000
snapshots:
  - 99
